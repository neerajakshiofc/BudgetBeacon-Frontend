# dependencies
/node_modules          # Ignore the node_modules directory (for both frontend and backend)

/.pnp
.pnp.js

# testing
/coverage              # Ignore test coverage reports (generated by testing tools)

# production
/build                 # Ignore build directory in the frontend (React, for example)

# misc
.DS_Store              # Ignore macOS-specific file
.env.local             # Ignore environment files for local development
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*         # Ignore npm log files generated during errors
yarn-debug.log*        # Ignore yarn log files
yarn-error.log*

node_modules/          # Ignore node_modules in both frontend and backend
.env                   # Ignore general .env file
build/                 # Ignore build/ directory in the frontend

frontend/node_modules/
frontend/build/
frontend/.env

backend/node_modules/
backend/.env
node_modules

.vercel
